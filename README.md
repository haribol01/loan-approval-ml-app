# loan-approval-ml-app
An end-to-end machine learning application that predicts loan approval using a custom-built logistic regression model. The system includes feature engineering, model evaluation, a FastAPI backend for real-time inference, and a responsive frontend interface for user interaction.
